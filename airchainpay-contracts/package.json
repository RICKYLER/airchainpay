{
  "name": "airchainpay-contracts",
  "version": "0.1.0",
  "description": "Smart contracts for AirChainPay offline crypto payment system.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "6.0.0",
    "hardhat": "2.25.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "17.0.0"
  }
}
